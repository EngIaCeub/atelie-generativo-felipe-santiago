# QA final - 2026-07-10

## Ambiente

- `python` do PATH: indisponível.
- `.venv` local: não usada porque aponta para runtime ausente.
- Python usado nos scripts do projeto:
  `C:\Users\felip\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -X utf8`.
- `pytest` e `ruff`: executados a partir de dependências temporárias em
  `C:\Users\felip\.codex\visualizations\2026\07\10\019f4a95-9a54-70f1-84d1-2254bfdc30fb\qa-deps`.

## Comandos e resultados

- `python scripts\collect_dataset.py --date 2026-07-10`: executado pelo Python empacotado; resultado final
  `Dataset preparado: 24 imagens em 768 x 768.`
- `python scripts\check_secrets.py`: executado pelo Python empacotado; resultado
  `Nenhum padrão de segredo detectado.`
- `python scripts\project_status.py`: executado pelo Python empacotado; `bootstrap` OK; `dataset` pendente
  apenas em `captions_reviewed` e `metadata`.
- `python scripts\validate_project.py --stage bootstrap`: executado pelo Python empacotado; resultado
  `Validação 'bootstrap' aprovada.`
- `python scripts\validate_project.py --stage dataset`: executado pelo Python empacotado; falhou como
  esperado com 2 pendências: `captions_reviewed` e `metadata`.
- `python -m compileall -q scripts app tests`: executado pelo Python empacotado; aprovado sem saída.
- `pytest -q`: `14 passed in 0.87s`.
- `ruff check scripts app tests`: `All checks passed!`

## Interpretação

O bootstrap está aprovado. A etapa de dataset está preparada até o primeiro portão humano real: revisão das
captions. `dados/metadata.jsonl` permanece vazio porque nenhuma caption foi revisada por pessoa humana.
